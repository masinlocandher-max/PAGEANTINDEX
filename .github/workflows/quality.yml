name: Repository quality

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Check PageantIndex browser JavaScript syntax
        run: |
          node --check public/launch-v2.js
          node --check public/claim-v2.js
          node --check platform/platform.js
          node --check public/experience.js
          node --check public/organization-experience.js
          node --check app/organizer.js
      - name: Run repository checks
        run: npm test
